import { platformViewsLoginPresenters } from './platform-views-login-presenters';

describe('platformViewsLoginPresenters', () => {
  it('should work', () => {
    expect(platformViewsLoginPresenters()).toEqual(
      'platform-views-login-presenters'
    );
  });
});
