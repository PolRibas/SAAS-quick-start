import { platformViewsLoginInfrastructureBrowser } from './platform-views-login-infrastructure-browser';

describe('platformViewsLoginInfrastructureBrowser', () => {
  it('should work', () => {
    expect(platformViewsLoginInfrastructureBrowser()).toEqual(
      'platform-views-login-infrastructure-browser'
    );
  });
});
