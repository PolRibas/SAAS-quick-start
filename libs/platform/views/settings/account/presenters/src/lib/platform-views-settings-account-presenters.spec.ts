import { platformViewsSettingsAccountPresenters } from './platform-views-settings-account-presenters';

describe('platformViewsSettingsAccountPresenters', () => {
  it('should work', () => {
    expect(platformViewsSettingsAccountPresenters()).toEqual(
      'platform-views-settings-account-presenters'
    );
  });
});
