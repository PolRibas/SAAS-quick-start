import { platformViewsSettingsCompanyUsersPresentationNest } from './platform-views-settings-company-users-presentation-nest';

describe('platformViewsSettingsCompanyUsersPresentationNest', () => {
  it('should work', () => {
    expect(platformViewsSettingsCompanyUsersPresentationNest()).toEqual(
      'platform-views-settings-company-users-presentation-nest'
    );
  });
});
