import { PlatformMessages } from "../../types"
import { menuMessages, errorMessages, labelsMessages, tablesMessages } from "./objects"

export const enUS: PlatformMessages = {
  menu: menuMessages,
  error: errorMessages,
  labels: labelsMessages,
  tables: tablesMessages,
}
